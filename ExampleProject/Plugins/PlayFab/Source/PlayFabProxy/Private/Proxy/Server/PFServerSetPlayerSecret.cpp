// This is automatically generated by PlayFab SDKGenerator. DO NOT modify this manually!
#include "PlayFabProxyPrivatePCH.h"
#include "PlayFab.h"
#include "PFServerSetPlayerSecret.h"

UPFServerSetPlayerSecret::UPFServerSetPlayerSecret(const FObjectInitializer& ObjectInitializer)
	: Super(ObjectInitializer)
	, SuccessDelegate(PlayFab::UPlayFabServerAPI::FSetPlayerSecretDelegate::CreateUObject(this, &ThisClass::OnSuccessCallback))
	, ErrorDelegate(PlayFab::FPlayFabErrorDelegate::CreateUObject(this, &ThisClass::OnErrorCallback))
{
}

UPFServerSetPlayerSecret* UPFServerSetPlayerSecret::SetPlayerSecret(UObject* WorldContextObject, class APlayerController* PlayerController , const FString& InPlayerSecret, const FString& InPlayFabId)
{
	UPFServerSetPlayerSecret* Proxy = NewObject<UPFServerSetPlayerSecret>();
 	//Proxy->PlayerControllerWeakPtr = PlayerController;
	Proxy->Request.PlayerSecret = InPlayerSecret;
	Proxy->Request.PlayFabId = InPlayFabId;
	

 	//Proxy->WorldContextObject = WorldContextObject;
	return Proxy;
}

void UPFServerSetPlayerSecret::Activate()
{
	// grab the module, so we can get a valid pointer to the client API 
	PlayFabServerPtr ServerAPI = IPlayFabModuleInterface::Get().GetServerAPI();

	bool CallResult = false;

	if(ServerAPI.IsValid())
	{	
		CallResult = ServerAPI->SetPlayerSecret(Request, SuccessDelegate, ErrorDelegate);
	}

	if(CallResult == false)
	{
		OnFailure.Broadcast();
	}
}


//////////////////////////////////////////////////////////////////////////
// Delegate handles
void UPFServerSetPlayerSecret::OnSuccessCallback(const PlayFab::ServerModels::FSetPlayerSecretResult& Result)
{
	OnSuccess.Broadcast();
}

void UPFServerSetPlayerSecret::OnErrorCallback(const PlayFab::FPlayFabError& InError)
{
	OnFailure.Broadcast();	
}
